To launch from the .sh files, open a terminal and type ./LaunchXXXX.sh
	You may have to mark the files as executable on your system with `chmod +x Launch*`
To launch from the bat files just double-click

To launch with a custom resolution run `java -jar Builder-1.1.jar x` where x is an integer st the intended width of the map in the window is 4320/x